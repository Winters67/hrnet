import React from 'react';
import Formulaire from '../../components/Formulaire/Formulaire';

const Home = () => {
    return (
        <div>
            <Formulaire/>
        </div>
    );
};

export default Home;